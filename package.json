{
  "name": "truestate-sales-management",
  "version": "1.0.0",
  "description": "Sales Management System for TruEstate SDE Intern Assignment",
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && node --max-old-space-size=4096 src/index.js",
    "frontend": "cd frontend && npm run dev",
    "build": "cd frontend && npm run build",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install && cd .."
  },
  "keywords": ["sales", "management", "dashboard", "react", "nodejs"],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
